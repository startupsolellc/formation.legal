---
/**
 * Header.astro — Glassmorphism navigation with scroll-aware styling.
 * Responsive nav for Route Engine pillar structure.
 */

import { SITE, NAV_ITEMS } from '../lib/constants';

const currentPath = Astro.url.pathname;
---

<header class="glass-header border-b border-[var(--color-border-subtle)] sticky top-0 z-50">
  <nav
    class="mx-auto flex max-w-6xl items-center justify-between px-4 py-3.5 sm:px-6"
    aria-label="Main navigation"
  >
    <!-- Logo / Brand -->
    <a
      href="/"
      class="flex items-center gap-2 text-lg font-bold tracking-tight text-[var(--color-primary-950)] hover:text-[var(--color-action)] transition-colors"
    >
      <!-- Minimal brand icon -->
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="size-5 text-[var(--color-action)]">
        <path d="M12 2 L2 7 L12 12 L22 7 Z" />
        <path d="M2 17 L12 22 L22 17" />
        <path d="M2 12 L12 17 L22 12" />
      </svg>
      {SITE.title}
    </a>

    <!-- Desktop Navigation -->
    <ul class="hidden lg:flex items-center gap-1" role="list">
      {NAV_ITEMS.map((item) => {
        const isActive =
          item.href === '/'
            ? currentPath === '/'
            : currentPath.startsWith(item.href);
        return (
          <li>
            <a
              href={item.href}
              class:list={[
                'relative px-3 py-2 text-sm font-medium rounded-[var(--radius-md)] transition-all',
                isActive
                  ? 'text-[var(--color-action)] bg-[var(--color-primary-50)]'
                  : 'text-[var(--color-muted)] hover:text-[var(--color-text-primary)] hover:bg-[var(--color-surface-alt)]',
              ]}
              aria-current={isActive ? 'page' : undefined}
            >
              {item.label}
            </a>
          </li>
        );
      })}
    </ul>

    <!-- Mobile hamburger button -->
    <button
      type="button"
      id="mobile-menu-toggle"
      class="lg:hidden flex items-center justify-center w-10 h-10 rounded-[var(--radius-md)] text-[var(--color-muted)] hover:bg-[var(--color-surface-alt)] hover:text-[var(--color-text-primary)] transition-all"
      aria-expanded="false"
      aria-controls="mobile-menu"
      aria-label="Toggle navigation menu"
    >
      <!-- Hamburger icon -->
      <svg id="icon-open" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="size-5">
        <path stroke-linecap="round" d="M4 6h16M4 12h16M4 18h16" />
      </svg>
      <!-- Close icon (hidden by default) -->
      <svg id="icon-close" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="size-5 hidden">
        <path stroke-linecap="round" d="M6 18L18 6M6 6l12 12" />
      </svg>
    </button>
  </nav>

  <!-- Mobile menu panel -->
  <div
    id="mobile-menu"
    class="lg:hidden hidden border-t border-[var(--color-border-subtle)]"
  >
    <ul class="px-4 py-3 space-y-1" role="list">
      {NAV_ITEMS.map((item) => {
        const isActive =
          item.href === '/'
            ? currentPath === '/'
            : currentPath.startsWith(item.href);
        return (
          <li>
            <a
              href={item.href}
              class:list={[
                'block rounded-[var(--radius-md)] px-3 py-2.5 text-sm font-medium transition-all',
                isActive
                  ? 'bg-[var(--color-primary-50)] text-[var(--color-action)]'
                  : 'text-[var(--color-muted)] hover:bg-[var(--color-surface-alt)] hover:text-[var(--color-text-primary)]',
              ]}
              aria-current={isActive ? 'page' : undefined}
            >
              {item.label}
            </a>
          </li>
        );
      })}
    </ul>
  </div>
</header>

<script>
  // Mobile menu toggle
  const toggle = document.getElementById('mobile-menu-toggle');
  const menu = document.getElementById('mobile-menu');
  const iconOpen = document.getElementById('icon-open');
  const iconClose = document.getElementById('icon-close');

  toggle?.addEventListener('click', () => {
    const expanded = toggle.getAttribute('aria-expanded') === 'true';
    toggle.setAttribute('aria-expanded', String(!expanded));
    menu?.classList.toggle('hidden');
    iconOpen?.classList.toggle('hidden');
    iconClose?.classList.toggle('hidden');
  });

  // Scroll-aware glassmorphism
  const header = document.querySelector('.glass-header');
  if (header) {
    const onScroll = () => {
      header.classList.toggle('scrolled', window.scrollY > 10);
    };
    window.addEventListener('scroll', onScroll, { passive: true });
    onScroll();
  }
</script>
